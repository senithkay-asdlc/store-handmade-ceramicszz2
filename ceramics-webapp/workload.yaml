apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: ceramics-webapp

endpoints:
  - name: http
    type: HTTP
    port: 9090
    basePath: /
    visibility:
      - external

dependencies:
  endpoints:
    - component: store-handmade-ceramicszz2-ceramics-api
      name: http
      visibility: project
      envBindings:
        address: CERAMICS_API_URL
  resources:
    - ref: store-handmade-ce-81a87f04
      envBindings:
        client_id: USER_AUTH_CLIENT_ID
        issuer: USER_AUTH_ISSUER
        jwks_url: USER_AUTH_JWKS_URL
        scopes: USER_AUTH_SCOPES
